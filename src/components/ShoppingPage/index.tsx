import React from "react";
import styled from "@emotion/styled";

function ShoppingPage() {
  return <Wrapper>Shopping</Wrapper>;
}

const Wrapper = styled.div({
  display: "flex",
  padding: "32px",
});

export default ShoppingPage;
